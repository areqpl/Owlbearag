# 🌐 Dynamic Command Auto-Discovery Engine

## Overview
The `CommandRegistryScanner` dynamically populates 389+ commands by scanning:
1. Agent Skills (`~/.agents/skills/**/*.md`)
2. MCP Server Tools
3. Owlbearag CLI Commands (`vps`, `gpu`, `rag`, `query`)
4. Remote System Utilities & SSH Commands
